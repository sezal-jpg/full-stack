npm create vite@latest product-card-app
cd product-card-app
npm install
npm run dev
import ProductCard from "./ProductCard";
import "./App.css";

function App() {
  return (
    <div className="container">
      <ProductCard
        name="Wireless Headphones"
        price="129.99"
        inStock={true}
      />

      <ProductCard
        name="Mechanical Keyboard"
        price="89.99"
        inStock={false}
      />

      <ProductCard
        name="Smart Watch"
        price="199.99"
        inStock={true}
      />
    </div>
  );
}
export default App;
function ProductCard({ name, price, inStock }) {
  return (
    <div className="card">
      <div className="image"></div>

      <h3>{name}</h3>
      <p className="price">${price}</p>

      {inStock ? (
        <span className="stock in">In Stock</span>
      ) : (
        <span className="stock out">Out of Stock</span>
      )}
    </div>
  );
}

export default ProductCard;
body {
  margin: 0;
  background: #f4f6fb;
  font-family: Arial, sans-serif;
}

.container {
  display: flex;
  justify-content: center;
  gap: 30px;
  margin-top: 60px;
}

.card {
  background: #fff;
  width: 220px;
  padding: 20px;
  border-radius: 18px;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
  text-align: center;
}

.image {
  height: 120px;
  background: #e1e5ea;
  border-radius: 14px;
  margin-bottom: 15px;
}

.price {
  font-size: 18px;
  font-weight: bold;
}

.stock {
  display: inline-block;
  margin-top: 10px;
  padding: 6px 14px;
  border-radius: 20px;
  font-size: 14px;
}

.in {
  background: #e6f7ec;
  color: #2e7d32;
}

.out {
  background: #fdecea;
  color: #c62828;
}
function ProductCard({ name, price, inStock }) {
  return (
    <div className="card">
      <h2>{name}</h2>
      <p className="price">₹{price}</p>

      <span className={inStock ? "stock in" : "stock out"}>
        {inStock ? "In Stock" : "Out of Stock"}
      </span>

      <button disabled={!inStock}>
        Add to Cart
      </button>
    </div>
  );
}

export default ProductCard;

import ProductCard from "./ProductCard";
import "./App.css";

function App() {
  return (
    <div className="container">
      <ProductCard name="Wireless Headphones" price="12999" inStock />
      <ProductCard name="Mechanical Keyboard" price="8999" inStock={false} />
      <ProductCard name="Smart Watch" price="19999" inStock />
    </div>
  );
}

export default App;
import ProductCard from "./ProductCard";
import "./App.css";

function App() {
  return (
    <div className="container">
      <ProductCard name="Wireless Headphones" price="12999" inStock />
      <ProductCard name="Mechanical Keyboard" price="8999" inStock={false} />
      <ProductCard name="Smart Watch" price="19999" inStock />
    </div>
  );
}

export default App;
import ProductCard from "./ProductCard";
import "./App.css";

function App() {
  return (
    <div className="container">
      <ProductCard name="Wireless Headphones" price="12999" inStock />
      <ProductCard name="Mechanical Keyboard" price="8999" inStock={false} />
      <ProductCard name="Smart Watch" price="19999" inStock />
    </div>
  );
}

export default App;
body {
  background: #f4f6f8;
  font-family: Arial, sans-serif;
}

.container {
  display: flex;
  gap: 30px;
  padding: 40px;
  justify-content: center;
  flex-wrap: wrap;
}

.card {
  background: white;
  padding: 25px;
  width: 260px;
  border-radius: 16px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.08);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

/* CARD HOVER ✨ */
.card:hover {
  transform: translateY(-10px) scale(1.02);
  box-shadow: 0 20px 40px rgba(0,0,0,0.15);
}

.price {
  font-size: 22px;
  font-weight: bold;
}

/* STOCK BADGE */
.stock {
  display: inline-block;
  padding: 6px 14px;
  border-radius: 20px;
  margin: 12px 0;
  font-size: 14px;
  transition: transform 0.3s ease;
}

.stock.in {
  background: #e6f8ec;
  color: #1b7f3b;
}

.stock.out {
  background: #fde8e8;
  color: #b91c1c;
}

/* BADGE HOVER */
.card:hover .stock {
  transform: scale(1.1);
}

/* BUTTON */
button {
  margin-top: 15px;
  width: 100%;
  padding: 10px;
  border: none;
  border-radius: 10px;
  background: black;
  color: white;
  cursor: pointer;
  transition: background 0.3s ease, transform 0.2s ease;
}


button:hover:not(:disabled) {
  background: #333;
  transform: scale(1.05);
}

button:disabled {
  background: #ccc;
  cursor: not-allowed;
}
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #f6f9fc;
}
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
function productcard({ name, price, inStock }) {
  return (
    <div className="card">
      <h2>{name}</h2>
      <p className="price">₹{price}</p>

      <span className={inStock ? "stock in" : "stock out"}>
        {inStock ? "In Stock" : "Out of Stock"}
      </span>

      <button disabled={!inStock}>
        Add to Cart
      </button>
    </div>
  );
}

export default ProductCard;
main.jsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
productcard.jsx
function productcard({ name, price, inStock }) {
  return (
    <div className="card">
      <h2>{name}</h2>
      <p className="price">₹{price}</p>

      <span className={inStock ? "stock in" : "stock out"}>
        {inStock ? "In Stock" : "Out of Stock"}
      </span>

      <button disabled={!inStock}>
        Add to Cart
      </button>
    </div>
  );
}

export default ProductCard;
app.jsx

import ProductCard from "./ProductCard";
import "./App.css";

function App() {
  return (
    <div className="container">
      <ProductCard name="Wireless Headphones" price="12999" inStock />
      <ProductCard name="Mechanical Keyboard" price="8999" inStock={false} />
      <ProductCard name="Smart Watch" price="19999" inStock />
    </div>
  );
}

export default App;
app.css
body {
  background: #f4f6f8;
  font-family: Arial, sans-serif;
}

.container {
  display: flex;
  gap: 30px;
  padding: 40px;
  justify-content: center;
  flex-wrap: wrap;
}

.card {
  background: white;
  padding: 25px;
  width: 260px;
  border-radius: 16px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.08);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

/* CARD HOVER ✨ */
.card:hover {
  transform: translateY(-10px) scale(1.02);
  box-shadow: 0 20px 40px rgba(0,0,0,0.15);
}

.price {
  font-size: 22px;
  font-weight: bold;
}

/* STOCK BADGE */
.stock {
  display: inline-block;
  padding: 6px 14px;
  border-radius: 20px;
  margin: 12px 0;
  font-size: 14px;
  transition: transform 0.3s ease;
}

.stock.in {
  background: #e6f8ec;
  color: #1b7f3b;
}

.stock.out {
  background: #fde8e8;
  color: #b91c1c;
}

/* BADGE HOVER */
.card:hover .stock {
  transform: scale(1.1);
}

/* BUTTON */
button {
  margin-top: 15px;
  width: 100%;
  padding: 10px;
  border: none;
  border-radius: 10px;
  background: black;
  color: white;
  cursor: pointer;
  transition: background 0.3s ease, transform 0.2s ease;
}


button:hover:not(:disabled) {
  background: #333;
  transform: scale(1.05);
}

button:disabled {
  background: #ccc;
  cursor: not-allowed;
}
index.css
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #f6f9fc;
}
